$(document).ready(function () {
    $('.slider').slick({
        dots: false,
        infinite: true,
        speed: 500,
        easing: 'linear',
        fade: true,
        adaptiveHeight: true,
        zIndex: 500,
        // nextArrow: document.querySelector('.slider__button-prev'),
        // prevArrow: document.querySelector('.slider__button-next'),
    });



});